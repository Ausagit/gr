Recommend adding the below to your .profile:


# Add scripts folder to PATH env variable
PATH=$PATH:$HOME/gr/scripts

# Aliases for 3k and 3s reconnecting TMUX with trcc script
alias 3k='trcc gr'
alias tt='tt++ ~/gr/.tinrc'

export PATH
